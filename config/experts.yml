version: 1
purpose: >
  Influential authors, maintainers, labs and research groups used for query
  expansion, attribution and signal weighting. This is not a forced inclusion
  list; candidates still need to pass the radar rubric.

default_read: false
read_when:
  - source_scope includes papers, arxiv, github, technical blogs or repo_activity
  - user asks for work from specific authors, labs, maintainers or research groups
  - candidate attribution is useful for ranking papers, blogs, projects or near-miss candidates
  - a focused scan needs stronger recall than channel profile and watchlist provide

usage_policy:
  primary_channel_match: use for query expansion and stronger attribution signal
  related_channel_match: use only as weak query expansion or tie-breaker signal
  no_channel_match: do not use unless the user explicitly asks for that expert
  not_a_retention_gate: true
  notes:
    - Expert match can raise attention, but cannot rescue a candidate with no technical evidence.
    - Prefer official pages, Google Scholar, DBLP, GitHub profiles and lab publication pages when adding entries.
    - Keep aliases short and useful for search; avoid broad organization names that belong in watchlist.yml.

schema:
  required_fields:
    - name
    - primary_channels
    - topics
  optional_fields:
    - type
    - affiliation
    - aliases
    - related_channels
    - links
    - notes
  allowed_types:
    - author
    - maintainer
    - lab
    - research_group

entry_defaults:
  type: author
  watch_for:
    - new paper
    - code release
    - benchmark
    - artifact
  notes: Keep entries lightweight; add Scholar/homepage links only when known exactly.

experts:
  - name: Song Han
    aliases: [Han Song]
    affiliation: MIT / NVIDIA
    primary_channels: [quantization]
    related_channels: [ai-infra]
    topics: [model compression, quantization, hardware-aware inference]

  - name: Tri Dao
    affiliation: Together AI
    primary_channels: [ai-infra]
    related_channels: [quantization]
    topics: [attention kernels, efficient attention, inference acceleration]

  - name: Zhijian Liu
    aliases: [Liu Zhijian]
    affiliation: UC San Diego
    primary_channels: [quantization]
    topics: [model compression, quantization, efficient inference]
    links:
      github: https://github.com/z-lab/

  - name: Dawei Yang
    aliases: [Yang Dawei]
    affiliation: Houmo AI
    primary_channels: [quantization]
    topics: [low-bit quantization, FP4, quantized training]

  - name: Zechun Liu
    aliases: [Liu Zechun]
    affiliation: Meta AI
    primary_channels: [quantization]
    topics: [model compression, quantization, efficient LLM]

  - name: Dan Alistarh
    affiliation: IST Austria
    primary_channels: [quantization]
    related_channels: [ai-infra]
    topics: [quantization, sparsity, distributed training]

  - name: Zhihang Yuan
    aliases: [Yuan Zhihang]
    affiliation: ByteDance
    primary_channels: [quantization]
    topics: [LLM quantization, post-training quantization, low-bit inference]

  - name: Haotong Qin
    aliases: [Qin Haotong]
    primary_channels: [quantization]
    topics: [LLM quantization, low-bit quantization, activation quantization]

  - name: Jianfei Chen
    aliases: [Chen Jianfei]
    affiliation: Tsinghua
    primary_channels: [quantization]
    related_channels: [ai-infra]
    topics: [efficient ML systems, LLM quantization, inference optimization]

  - name: Christopher De Sa
    aliases: [Chris De Sa]
    affiliation: Cornell
    primary_channels: [quantization]
    topics: [low-precision training, quantization, optimization]
